server:
  port: 8072
spring:
  application:
    name: consumer-app-one
  activemq:
    broker-url: tcp://localhost:61616
    user: admin
    password: admin
    packages:
      trust-all: true
  jms:
    topic-name: "topic.st03"

logging:
  level:
    root: error
    com:
      tananushka:
        mom:
          consumerapp:
            one:
              consumer: info